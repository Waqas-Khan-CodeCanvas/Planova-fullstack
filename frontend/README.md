axiosInstance path 
install mongodb comunity server
c:data/db
mongod --version then mongod
add todos in all backend for protected routes

authRoute.js register fix this

api test:

    register : post(http://localhost:5000/api/auth/register)
    data({
  "name": "zaryab",
  "email": "zaryab@gmail.com",
  "password": "123456"
})

same for login 
http://localhost:5000/api/users to get all users


title controller in taskController tittle -> title